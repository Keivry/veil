## 1. 骨架、状态搬迁与首消费者（前置）

- [x] 1.1 新建 `src/handler/llm/pump/spawn/terminator.rs`<!-- doc-paths-ignore -->：定义 `StreamTerminator`（私有 `TerminalState` + 私有位 `loop_terminated`/`rejected_sticky`/`audit_blocked`/`responses_failed_seen`/`any_frame_sent`）+ `TerminalKind`/`TerminalPlan`。类型与 impl SHALL 声明 `pub(in crate::handler::llm::pump)`（MAJOR-4 选项 (a)：测试模块 `pump::terminator_tests` 是 `pump::spawn` 的**兄弟**，`pub(super)` 无法命名；测试保持在 `src/handler/llm/pump/terminator_tests.rs`）。`TerminalPlan` 为 `None | Frames { kind: TerminalKind, frames: Vec<String>, truncated: Option<TruncatedMode> }`（MAJOR-6：`None` 仅表「未开放/已终端」，零合成帧用空 `frames`，不得混用）。实现访问器 `is_open`/`terminal_sent`/`block_injected`/`terminated`/`any_frame_sent`/`rejected_sticky`/`audit_blocked`/`responses_failed_seen`（MAJOR-7）与变更器 `note_frame_sent`/`mark_upstream_terminal`/`note_sticky_rejected`/`note_responses_failed`/`mark_loop_terminated`，以及 `plan_block`/`plan_midstream`（签名含 `metrics: Option<&GatewayMetrics>`，MAJOR-5）/`plan_empty_stream`/`plan_responses_error`/`commit`。`plan_*` 在 `!is_open()` 时返回 `TerminalPlan::None`；`commit(kind, frames_sent, terminal_frame_delivered)` 为终端帧位与 `meta.terminal_injected` 唯一回填点（逐 kind 语义见 design §2.1 表）；`frames_sent` = 收尾是否成立，`terminal_frame_delivered` = 终端帧是否实际下行（决定 `terminal_injected`），二者 SHALL NOT 合并。**中间态 `dead_code` 登记（MAJOR-8）**：迁移窗口内无生产消费者的类型/impl 加 `#[cfg_attr(not(test), allow(dead_code))]`（内联 `#[cfg(test)]` 单测**不能**为非测试 lib target 消除告警），同处注释登记「消费者全部落位后由 3.3 移除」；除该登记项外**不得**使用无登记的 `#[allow(dead_code)]`。在 `src/handler/llm/pump/spawn.rs:5-10` 注册 `mod terminator;`
  - 验证：`cargo build` 与 `cargo clippy --tests --all-targets -- -D warnings` 通过（中间态 `allow` 已按 design §2.3 登记）；`python3 scripts/check_file_sizes.py` exit 0（`terminator.rs` ≤800 行）

- [x] 1.2 `src/handler/llm/pump/spawn/setup.rs:49-76` 的 `PumpLoopState` 删去 7 枚终端相关 bool（`any_frame_sent`/`terminated`/`rejected_sticky`/`block_injected`/`audit_blocked`/`terminal_sent`/`responses_failed_sent`），改持 `terminator: StreamTerminator`；`:115-138` 初始化同步；读点改访问器（MAJOR-7 全覆盖）——`event_loop.rs:119`（`terminated()`）、`:230/:277/:358/:756`（`terminal_sent()`）、`:251/:263/:366`（`rejected_sticky()`）、`:285`（`responses_failed_seen()`，经 `decide::responses_control_action`，`decide.rs:25-41`）、`decide.rs:71-85`/`event.rs:79`（`is_open()`/`any_frame_sent()`/`block_injected()`）、`finish.rs:70`（`audit_blocked()`）、`finish.rs:74`（`block_injected()`）；写点由 2.1/2.2/3.1/3.2/4 接管，`responses_failed_sent` 写点（`:295`/`:350`）改 `note_responses_failed()`
  - 验证：`cargo test -p veil` 全绿（行为不变）；`cargo clippy --tests --all-targets -- -D warnings` exit 0（无 `dead_code`/未用字段）

## 2. 阻断注入收敛（I-1/I-2，见 design §1.1）

- [x] 2.1 收敛流内阻断臂：`src/handler/llm/pump/spawn/event_loop.rs:137-176` 的 `apply_reject_block` 改为 `note_sticky_rejected()` 后调 `terminator.plan_block(protocol, reason, conv_id, blocked_index, seq_cursor, Some(&env.metrics))`；调用点循环 `send` 后 `terminator.commit(TerminalKind::Block, true)`；**计数口径逐点保持（BLOCKER-1）**：I-1 现状**不含** `add_sse_event`，收敛后 SHALL NOT 新增任何帧级计数；**保持** `:150-159` 的 pending 缓冲归还/清空顺序与返回值 `is_tool_or_complete` 语义不变；删除 `:160-173` 的 `protocol_block_frames` 直构
  - 验证：`cargo test -p veil direct_rejected_sticky_suppresses_tool_frames`（`spawn_tests.rs:501`）与 `cargo test -p veil spawn_decision_sticky_suppress_truth_table`（`:300`）通过；code review：`grep -rn "protocol_block_frames" src/handler/llm/pump/spawn/event_loop.rs` 仅允许命中注释/文档行（`//`/`///`），出现任何**调用代码行**即未完成（生产段不再直构）

- [x] 2.2 收敛收尾阻断臂：`src/handler/llm/pump/spawn/terminal.rs:152-176` 改为调 `terminator.plan_block(...)`（`metrics` 参数保持现状 `None`）+ 调用点 `send` + `terminator.commit(TerminalKind::Block, true)`（I-2 现状即无条件置位、忽略 `send` 结果，`:153`/`:171`，保持）；**计数口径逐点保持（BLOCKER-1）**：I-2 现状**不含** `add_sse_event`，收敛后 SHALL NOT 新增；删除 `:161-168` 直构；`TerminalCtx`（`:32-67`）删去 `terminal_sent`/`block_injected`/`any_frame_sent` 借用字段，改经 `StreamTerminator` 访问器；`terminal.rs:153-157` 的清缓冲顺序与 `:171` 的 `mark_terminal` 经 `commit` 完成
  - 验证：`cargo test -p veil synth_terminal_single`（`spawn_tests.rs:683`）与 `cargo test -p veil synth_terminal_flush_order`（`:645`）通过；code review：`grep -rn "protocol_block_frames" src/handler/llm/pump/spawn/terminal.rs` 仅允许注释/文档行，出现任何调用代码行即未完成

## 3. 终止帧收敛（I-3/I-4/I-5 + 上游终端，见 design §1.1）

- [x] 3.1 收敛中途断流终端帧选择：`src/handler/llm/pump/synth_flush.rs:71-160` 的 `midstream_terminal` 帧选择迁入 `terminator.plan_midstream(protocol, conv_id, clean_close, seq_cursor, Some(metrics))`（MAJOR-5：Responses 臂需 `metrics`，以在 `conv_id` 缺失时经 `resolve_conv_id(None, &Value::Null, Some(metrics), "truncated")`（`src/service/llm_gateway/tool.rs:740-742`）记 `record_conv_missing` 并取归档回退 id；Chat 用 `chat_done_frame` `src/service/block_inject/frames.rs:303`、Responses 用 `synthesize_truncation` `frames.rs:485-498`、Anthropic 返回 `Frames { frames: vec![], truncated: Some(TruncatedMode::OpenEnded), .. }`（零合成帧即合法终止））；`flush_pre_terminal`（`synth_flush.rs:23-43`）与 `MidstreamTerminalOutcome`（`:48-51`）保留；调用点按 plan 的 `truncated` 字段落 `set_truncated`，**保持 `synth_flush.rs:39`（`flush_pre_terminal`，I-5 复用）`/`:98`/`:137` 既有 `add_sse_event` 计数不变（BLOCKER-1）**；`terminal.rs:255-280` 调用点先算 `delivered = !frames.is_empty() && mid.terminal_sent`（不得用 `mid.forwarded`，其含 `flush_pre_terminal` 滞留帧，非终端帧；Anthropic 零合成帧时 `delivered=false`），再以 `terminator.commit(TerminalKind::Midstream, mid.terminal_sent, delivered)` 回填（`send` 失败不置位语义不变；零合成帧收尾成立但不置 `terminal_injected`）
  - 验证：`cargo test -p veil truncation_terminal_flush_order`（`spawn_tests.rs:720`）、`cargo test -p veil truncation_send_failure_guard`（`:761`）通过；`PumpOutcome.forwarded` 与既有断言逐项一致

- [x] 3.2 收敛真空流与 Responses error 与上游终端：`terminal.rs:283-312` 真空流改 `terminator.plan_empty_stream`（`empty_stream_frames` `frames.rs:505-512`）+ `commit(TerminalKind::EmptyStream, frames_sent)`，**保持 I-4 现状无 `add_sse_event`（BLOCKER-1）**且空帧集仅落 `set_truncated(OpenEnded)`、非空帧集先 `commit` 再落对应 truncation；`event_loop.rs:290-343` 的 `type:"error"` 单帧改 `terminator.plan_responses_error`（`responses_failed_frame` `frames.rs:180-199`）+ **`commit(TerminalKind::ResponsesError, terminal_ok)`（BLOCKER-3：传实际 `terminal_ok`，不得硬编码 `true`；`send` 失败即不置终端帧位）**，其后另调 `terminator.mark_loop_terminated()`（对齐 `:341` 无条件置 `terminated`）；`ResponsesAction::DuplicateFailed`（`:344-347`）改仅调 `terminator.mark_loop_terminated()`（无帧发送，对齐 `:345`）；**保持 `event_loop.rs:332` 既有 `add_sse_event` 计数不变（BLOCKER-1）**；`event_loop.rs:622-624`（`event_terminal`）与 `:753-772`（`[DONE]`）改 `terminator.mark_upstream_terminal()`
  - 验证：`cargo test -p veil direct_n1_completed_then_error_single_terminal`（`spawn_tests.rs:378`）与 `cargo test -p veil spawn_decision_empty_stream_gate_call_order`（`:360`）通过；code review：`grep -rn "responses_failed_frame" src/handler/llm/pump/spawn/event_loop.rs` 仅允许注释/文档行，出现任何调用代码行即未完成

- [x] 3.3 移除中间态 `allow`（MAJOR-8 到期义务）：全部 `plan_*` 消费者（2.1/2.2/3.1/3.2）落位后，删除 `terminator.rs` 中 `#[cfg_attr(not(test), allow(dead_code))]` 与登记注释
  - 验证：`grep -rn "allow(dead_code)" src/handler/llm/pump/spawn/terminator.rs`<!-- doc-paths-ignore --> 命中为 0；`cargo clippy --tests --all-targets -- -D warnings` exit 0（无 `dead_code`）

## 4. 纯谓词接线与不变量守护

- [x] 4.1 纯谓词保持纯函数、仅换入参来源：`src/handler/llm/pump/decide.rs:25-41`（`responses_control_action`）及其调用点 `event_loop.rs:283-288`、`decide.rs:71-85`（`should_apply_midstream_terminal`）、`:54-66`（`sticky_suppress_action`）、`src/handler/llm/pump/event.rs:79`（`should_synthesize_empty_stream`）改取 `terminator.is_open()`/`terminal_sent()`/`block_injected()`/`terminated()`/`any_frame_sent()`/`rejected_sticky()`/`responses_failed_seen()`（MAJOR-7）；`src/handler/llm/pump/spawn/finish.rs:70,74` 改经 `terminator.audit_blocked()`/`terminator.block_injected()` 组装 `PumpOutcome`（`src/handler/llm/pump.rs:88-95` 字段名与语义不变）。**行数约束（MINOR-12）**：`event.rs` 现 787 行（余量 13），对 `event.rs:79` 的改写 SHALL **不得新增行**（等行替换仅换入参来源）
  - 验证：`cargo test -p veil spawn_decision_midstream_terminal_gate_truth_table`（`spawn_tests.rs:87`）、`cargo test -p veil spawn_decision_empty_stream_gate_call_order` 通过；`PumpOutcome.terminal_injected`/`block_injected` 断言（`stream_tests.rs:424,476` 等）全绿

- [x] 4.2 新增测试文件 `src/handler/llm/pump/terminator_tests.rs`<!-- doc-paths-ignore -->（在 `src/handler/llm/pump.rs:33-46` 注册 `#[cfg(test)] mod terminator_tests;`）：`stream_terminator_exact_one_terminal_matrix`（三协议 × {阻断, 中途断流, 真空流, 上游 error}，经 `collect_pump` 断言**每路径终端闭合恰一次**，按协议分别为——Chat 阻断/中途断流/真空流各恰一台成 `data: [DONE]`，Chat 上游 error 零合成帧（error 帧自身终端，记 `upstream_error`）；Responses 阻断恰一 `response.completed`、中途断流/上游 error 恰一 `response.failed`、真空流全序列尾帧恰一 `response.failed`；Anthropic 阻断恰一 `message_stop`、真空流 `message_start`+`message_stop` 各恰一、**中途断流零合成终端帧 + 记 `open_ended`**并断言 `!outcome.terminal_injected`（对齐 `veil-stream-fidelity-fix` D9/S9：零合成帧不置注入标记）；「零合成帧」计为合法终止一次，测试**不得**靠伪造帧满足「恰一」）+ `stream_terminator_injection_idempotent`（已终端后 `plan_*` 返回 `TerminalPlan::None`，不产生第二终端）；复用 `stream_tests` 的 `collect_pump`/`fresh_arcs`/`loopback_server`/`pump_ctx`（`src/handler/llm/stream_tests.rs:29/37/71/148`）
  - 验证：`cargo test -p veil stream_terminator_exact_one_terminal_matrix` 与 `cargo test -p veil stream_terminator_injection_idempotent` 通过；`python3 scripts/check_file_sizes.py` exit 0（`terminator_tests.rs` ≤800、`spawn_tests.rs` 行数不增长、`event.rs` 不增长）

## 5. 文档与 spec 同步

- [x] 5.1 本 change delta `specs/architecture-cleanup/spec.md` 的 requirement 正文与 design §2/§3 一致（单所有者/单入口/幂等/计数按站点逐点保持/纯重构行为保持）；canonical `openspec/specs/architecture-cleanup/spec.md` 于归档期晋升（本 change 仅 `## ADDED Requirements`，不改既有 requirement header/场景名）
  - 验证：`openspec validate veil-stream-terminator-convergence --strict` 通过

- [x] 5.2 design §1.2 的 flag 集合与 apply 后实际写点一致：若实现期发生改名/合并/删除，须同批修订 `design.md` 与 delta spec，**不静默漂移**
  - 验证（code review criterion，非自指 grep）：逐项将 `design.md` §1.2 的「写点/读点」列与实际源码对照——对 7 枚位逐一 `grep -rn "<flag>" src/handler/llm/pump/spawn/event_loop.rs src/handler/llm/pump/spawn/terminal.rs src/handler/llm/pump/synth_flush.rs src/handler/llm/pump/spawn/finish.rs`，写点须全部经 `StreamTerminator` 变更器、读点须全部经访问器；任何直写/直读残留或表项与源码不符即未完成，须同批修订表项

## 验证门禁（Verification Gate）

- [x] G-1 `bash scripts/gate.sh` 七步全绿（fmt / clippy `-D warnings` / test / doc-paths / file-sizes / conformance / go vet+test），任一步非零退出即本 change 未完成
- [x] G-2 `openspec validate veil-stream-terminator-convergence --strict` 通过；`openspec validate --all --strict` 亦通过
- [x] G-3 `python3 scripts/check_doc_paths.py` exit 0；本 change 内所有 `src/...rs:NNN` 锚点存在且在界；规划期新模块路径以 `<!-- doc-paths-ignore -->` 标注
- [x] G-4 `python3 scripts/check_file_sizes.py` exit 0；`event_loop.rs`/`terminal.rs`/`synth_flush.rs` 与新增 `terminator.rs`/`terminator_tests.rs` 均 ≤800 行，`spawn_tests.rs` 不增长，`event.rs`（787）不增长
- [x] G-5 归档晋升：apply 完成后按 r2/r3/r4 先例将本 change 的 `specs/architecture-cleanup/spec.md` delta 晋升 canonical `openspec/specs/architecture-cleanup/spec.md`（同批直改 + 归档 early-sync no-op）
- [x] G-6 本 change 规划期（artifacts-only）不改 `src/**`、`tests/**`、`scripts/**`、`README.md`、`openspec/specs/**` 与任何其他 change 目录；不归档、不 `git add/commit`、不运行 `cargo`
